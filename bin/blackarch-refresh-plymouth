#!/bin/bash

sudo cp ~/.local/share/blackarch/default/plymouth/* /usr/share/plymouth/themes/blackarch/
sudo plymouth-set-default-theme blackarch

if command -v limine-mkinitcpio &>/dev/null; then
  sudo limine-mkinitcpio
else
  sudo mkinitcpio -P
fi
