#!/bin/bash

# Call the VSCode theme setter with Cursor-specific parameters
blackarch-theme-set-vscode cursor "$HOME/.config/Cursor/User/settings.json" "$HOME/.local/state/blackarch/toggles/skip-cursor-theme-changes" Cursor
